const templates = {
    projectManager: {
    	
        template1: `
            <h1>${data.title}</h1>
            <p>${data.content}</p>
        `,

        template2: `
            <h2>${data.subtitle}</h2>
            <p>${data.description}</p>
        `
        // Agrega más plantillas aquí según sea necesario
    }
};


